version: 2
jobs:
  build:
        machine: true
        steps:
            - checkout
            - run:
                name: Install python
                command: |
                  apt install -y python3 python
